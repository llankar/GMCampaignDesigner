
## OpenClaw memory flush - 2026-03-24
- Nettoyage du repo effectué : suppression des __pycache__, renforcement de .gitignore, correction de collisions de noms dans les tests, allègement de plusieurs __init__.py via imports paresseux, et retrait d'effets de bord à l'import (theme/app appearance déplacés au démarrage explicite de MainWindow).
- Changements poussés sur GitHub avec le commit 62c6b3d (efactor: remove import side effects and fix test collisions).
- Bug corrigé dans modules/scenarios/gm_screen_view.py : open_entity_tab gérait mal le type Maps (KeyError car Maps n'est pas dans self.wrappers). Fix poussé avec le commit f90ff80 (ix: handle Maps in gm screen entity tabs).
- Problème UI corrigé dans le GM screen : le synopsis du scénario ne remplissait pas toute la largeur disponible à cause d'un wraplength fixe dans create_hero_header. Wrap adaptatif ajouté et poussé avec le commit 536381e (ix: expand gm screen synopsis wrap to available width).
- Préférence utilisateur explicite : quand une tâche est finie, pousser les modifications sur le dépôt si c'est propre ; rester prudent si un push risque d'embarquer des changements sensibles ou trop larges.

