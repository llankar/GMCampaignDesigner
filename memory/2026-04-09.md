- Implemented a new `GM Table` concept as a freeform GM workspace with draggable/resizable panels, scenario-scoped autosave, and top-bar menu integration.
- Main entry point is `main_window.open_gm_table`; core UI lives in `modules/scenarios/gm_table_view.py` and `modules/scenarios/gm_table/`.
- Targeted tests added for top-bar menu exposure and GM Table layout persistence.
